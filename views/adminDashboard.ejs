<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Command Center | GigSetu</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: white;
            min-height: 100vh;
            padding-top: 40px;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            transition: 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .stat-card:hover {
            background: rgba(255, 255, 255, 0.15);
            transform: translateY(-5px);
        }

        .stat-value {
            font-size: 28px;
            font-weight: 700;
            display: block;
            color: #00c6ff;
        }

        .stat-label {
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 1px;
            opacity: 0.7;
        }

        .admin-btn {
            width: 100%;
            padding: 15px;
            border-radius: 12px;
            border: none;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            transition: all 0.3s;
            text-decoration: none;
            color: white;
            background: rgba(255, 255, 255, 0.1);
            margin-bottom: 15px;
        }

        .admin-btn:hover {
            background: rgba(255, 255, 255, 0.2);
            color: #00c6ff;
            transform: scale(1.02);
        }

        .btn-success-glow { background: linear-gradient(45deg, #28a745, #20c997); box-shadow: 0 4px 15px rgba(40, 167, 69, 0.3); }
        .btn-warning-glow { background: linear-gradient(45deg, #f39c12, #e67e22); box-shadow: 0 4px 15px rgba(243, 156, 18, 0.3); }
        .btn-danger-glow { background: rgba(220, 53, 69, 0.2); border: 1px solid #dc3545; }
        
        .header-title {
            font-weight: 700;
            letter-spacing: -1px;
            background: linear-gradient(to right, #fff, #00c6ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
    </style>
</head>
<body>

<div class="container mb-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="header-title">👑 Admin Command Center</h1>
        <a href="/logout" class="btn btn-outline-danger px-4 py-2" style="border-radius: 10px;">
            <i class="bi bi-box-arrow-right"></i> Logout
        </a>
    </div>

    <div class="glass-card">
        <h4 class="mb-4"><i class="bi bi-graph-up-arrow me-2"></i> Platform Overview</h4>
        <div class="row g-3">
            <div class="col-md-4">
                <div class="stat-card">
                    <span class="stat-label">Total Workers</span>
                    <span class="stat-value"><%= totalUsers %></span>
                </div>
            </div>
            <div class="col-md-4">
                <div class="stat-card">
                    <span class="stat-label">Verified</span>
                    <span class="stat-value" style="color: #2ecc71;"><%= verifiedUsers %></span>
                </div>
            </div>
            <div class="col-md-4">
                <div class="stat-card">
                    <span class="stat-label">Pending</span>
                    <span class="stat-value" style="color: #f1c40f;"><%= pendingUsers %></span>
                </div>
            </div>
            <div class="col-md-6">
                <div class="stat-card">
                    <span class="stat-label">Platform Earnings</span>
                    <span class="stat-value text-white">₹<%= totalEarnings %></span>
                </div>
            </div>
            <div class="col-md-6">
                <div class="stat-card">
                    <span class="stat-label">Total Wallet Funds</span>
                    <span class="stat-value text-white">₹<%= totalWalletFunds %></span>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="glass-card h-100">
                <h4 class="mb-4"><i class="bi bi-person-gear me-2"></i> User & Verification</h4>
                <a href="/admin/verifications" class="admin-btn">
                    <i class="bi bi-shield-check"></i> Manage Verifications
                </a>
                <a href="/admin/users" class="admin-btn">
                    <i class="bi bi-people"></i> Manage User Directory
                </a>
                <a href="/insurance/admin/claims" class="admin-btn">
                    <i class="bi bi-file-earmark-medical"></i> Insurance Claims
                </a>
            </div>
        </div>

        <div class="col-md-6">
            <div class="glass-card h-100">
                <h4 class="mb-4"><i class="bi bi-shield-lock me-2"></i> Financial & Risk</h4>
                <a href="/admin/loans" class="admin-btn btn-success-glow text-white">
                    <i class="bi bi-cash-stack"></i> Manage Loan Applications
                </a>
                <a href="/admin/risk" class="admin-btn btn-warning-glow text-white">
                    <i class="bi bi-exclamation-triangle"></i> Risk & Defaulters Panel
                </a>
                <a href="/admin/fraud" class="admin-btn">
                    <i class="bi bi-incognito"></i> Fraud Monitoring
                </a>
            </div>
        </div>
    </div>
</div>

</body>
</html>