<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loan Underwriting | GigSetu</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: white;
            min-height: 100vh;
            padding: 40px 0;
        }

        .glass-panel {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.4);
        }

        .loan-table {
            color: white;
            vertical-align: middle;
        }

        .loan-table thead th {
            background: rgba(255, 255, 255, 0.05);
            border: none;
            text-transform: uppercase;
            font-size: 11px;
            letter-spacing: 1.5px;
            padding: 15px;
            color: #00c6ff;
        }

        .loan-row {
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            transition: background 0.3s;
        }

        .loan-row:hover {
            background: rgba(255, 255, 255, 0.03);
        }

        /* Risk Tier Badges */
        .tier-badge {
            padding: 4px 10px;
            border-radius: 6px;
            font-size: 11px;
            font-weight: 700;
        }
        .tier-LOW { background: rgba(46, 204, 113, 0.2); color: #2ecc71; border: 1px solid #2ecc71; }
        .tier-MEDIUM { background: rgba(241, 196, 15, 0.2); color: #f1c40f; border: 1px solid #f1c40f; }
        .tier-HIGH { background: rgba(231, 76, 60, 0.2); color: #e74c3c; border: 1px solid #e74c3c; }

        .form-control-dark {
            background: rgba(0, 0, 0, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
            font-size: 13px;
            border-radius: 8px;
        }

        .form-control-dark:focus {
            background: rgba(0, 0, 0, 0.3);
            border-color: #00c6ff;
            color: white;
            box-shadow: none;
        }

        .btn-approve {
            background: #2ecc71;
            border: none;
            color: white;
            font-weight: 600;
            padding: 8px 16px;
            border-radius: 8px;
            transition: 0.3s;
        }

        .btn-approve:hover { background: #27ae60; transform: translateY(-2px); }

        .btn-reject {
            background: transparent;
            border: 1px solid rgba(231, 76, 60, 0.5);
            color: #e74c3c;
            padding: 8px 16px;
            border-radius: 8px;
            transition: 0.3s;
        }

        .btn-reject:hover { background: #e74c3c; color: white; }

        .back-link {
            text-decoration: none;
            color: rgba(255,255,255,0.6);
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 20px;
        }

    </style>
</head>
<body>
    <%- include("partials/adminHeader", { title: "Home - GigSetu" }) %>

<div class="container-fluid px-5">
    <a href="/admin/dashboard" class="back-link">
        <i class="bi bi-arrow-left"></i> Command Center
    </a>

    <div class="glass-panel">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <div>
                <h2 class="fw-bold mb-1">💼 Loan Application Queue</h2>
                <p class="text-info mb-0">Underwriting pipeline for gig-worker micro-credit</p>
            </div>
            <div class="text-end">
                <span class="d-block opacity-50 small">ACTIVE APPLICATIONS</span>
                <h3 class="fw-bold text-white"><%= loans.length %></h3>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table loan-table">
                <thead>
                    <tr>
                        <th>Worker Details</th>
                        <th>Principal</th>
                        <th>Tenure</th>
                        <th>Risk Tier</th>
                        <th>Rate / EMI</th>
                        <th class="text-center">Decision Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% loans.forEach(loan => { %>
                        <tr class="loan-row">
                            <td>
                                <div class="fw-bold"><%= loan.user.name %></div>
                                <small class="opacity-50 text-uppercase" style="font-size: 10px;">Verified Profile</small>
                            </td>
                            <td>
                                <span class="fw-bold">₹<%= loan.amount.toLocaleString() %></span>
                            </td>
                            <td>
                                <span class="badge bg-dark"><%= loan.tenure %> Months</span>
                            </td>
                            <td>
                                <span class="tier-badge tier-<%= loan.riskTier %>">
                                    <%= loan.riskTier %>
                                </span>
                            </td>
                            <td>
                                <div class="small">Int: <span class="text-info"><%= loan.interestRate %>%</span></div>
                                <div class="fw-bold text-success">₹<%= loan.emiAmount %>/mo</div>
                            </td>
                            <td>
                                <div class="d-flex gap-2 justify-content-center align-items-center">
                                    <form method="POST" action="/admin/loans/approve/<%= loan._id %>" class="d-flex gap-2">
                                        <% if (loan.riskTier === "HIGH") { %>
                                            <input type="text" name="overrideReason" 
                                                   class="form-control form-control-dark" 
                                                   placeholder="Risk Override Reason" required />
                                        <% } %>
                                        <button type="submit" class="btn-approve">
                                            <i class="bi bi-check-circle-fill"></i> Approve
                                        </button>
                                    </form>

                                    <form method="POST" action="/admin/loans/reject/<%= loan._id %>">
                                        <button type="submit" class="btn-reject">
                                            Reject
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</div>

</body>
<%- include("partials/footer") %>
</html>