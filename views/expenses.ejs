<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Ledger | GigSetu</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #ffffff;
            min-height: 100vh;
            padding: 40px 0;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 25px;
            margin-bottom: 20px;
        }

        .summary-title {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            opacity: 0.7;
        }

        .summary-value {
            font-size: 1.5rem;
            font-weight: 700;
        }

        .tax-highlight {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.3);
            border-radius: 15px;
            padding: 15px;
        }

        .form-control, .form-select {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
            border-radius: 10px;
            padding: 12px;
        }

        .form-select option {
            background-color: #203a43; 
            color: #ffffff;
        }

        .form-control:focus, .form-select:focus {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border-color: #00c6ff;
            box-shadow: none;
        }

        .expense-item {
            background: rgba(255, 255, 255, 0.03);
            border-left: 4px solid #00c6ff;
            border-radius: 12px;
            padding: 15px;
            margin-bottom: 12px;
            transition: 0.3s;
            position: relative;
        }

        .expense-item:hover {
            background: rgba(255, 255, 255, 0.07);
        }

        .cat-icon {
            width: 40px;
            height: 40px;
            background: rgba(0, 198, 255, 0.1);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #00c6ff;
        }

        .btn-primary-custom {
            background: linear-gradient(45deg, #00c6ff, #0072ff);
            border: none;
            font-weight: 600;
            border-radius: 10px;
        }

        .btn-delete {
            background: transparent;
            border: none;
            color: rgba(255, 71, 87, 0.5);
            transition: 0.3s;
        }
        .btn-delete:hover { color: #ff4757; }

        .itr-link {
            text-decoration: none;
            color: #93c5fd;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            margin-bottom: 8px;
        }

        .itr-link:hover { color: #fff; }
    </style>
</head>
<body>

<div class="container">
    <div class="row align-items-center mb-4">
        <div class="col-6">
            <h2 class="fw-bold mb-0">💸 Expense Ledger</h2>
        </div>
        <div class="col-6 text-end">
            <a href="/dashboard" class="btn btn-sm btn-outline-light rounded-pill px-3">
                <i class="bi bi-house me-1"></i> Dashboard
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-5">
            <div class="glass-card shadow-sm">
                <h5 class="fw-bold mb-4">Tax Intelligence Summary</h5>
                <div class="row g-3">
                    <div class="col-6">
                        <span class="summary-title">Income</span>
                        <div class="summary-value text-white">₹<%= monthlyIncome %></div>
                    </div>
                    <div class="col-6">
                        <span class="summary-title">Expenses</span>
                        <div class="summary-value text-info">₹<%= monthlyExpenseTotal %></div>
                    </div>
                </div>
                
                <div class="tax-highlight mt-4">
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="small opacity-75">Est. Tax Liability</span>
                        <span class="fw-bold text-success">₹<%= estimatedTax %></span>
                    </div>
                    <% if (taxReminderMessage) { %>
                        <div class="small mt-2 text-info">
                            <i class="bi bi-bell-fill me-1"></i> <%= taxReminderMessage %>
                        </div>
                    <% } %>
                </div>

                <div class="mt-4">
                    <a href="/expenses/export/csv" class="btn btn-primary-custom w-100 py-2">
                        <i class="bi bi-download me-2"></i> Export Tax Report (CSV)
                    </a>
                </div>
            </div>

            <div class="glass-card">
                <h5 class="fw-bold mb-3">Add New Expense</h5>
                <form method="POST" action="/expenses/add">
                    <div class="mb-3">
                        <label class="small opacity-50 mb-1">Category</label>
                        <select name="category" class="form-select shadow-none" required>
                            <option value="fuel">⛽ Fuel & Transport</option>
                            <option value="repair">🛠️ Vehicle Repair</option>
                            <option value="rent">🏠 Workplace/Room Rent</option>
                            <option value="food">🍱 Meals & Water</option>
                            <option value="mobile">📱 Data & Calls</option>
                            <option value="other">📦 Miscellaneous</option>
                        </select>
                    </div>
                    <div class="input-group mb-3">
                        <span class="input-group-text bg-transparent border-end-0 text-white-50">₹</span>
                        <input type="number" name="amount" class="form-control border-start-0 shadow-none text-white" placeholder="Amount" required style="background: rgba(255,255,255,0.05);">
                    </div>
                    <div class="mb-3">
                        <input type="text" name="note" class="form-control shadow-none text-white" placeholder="Note (e.g. Monthly Petrol)" style="background: rgba(255,255,255,0.05);">
                    </div>
                    <button class="btn btn-outline-info w-100 fw-bold py-2">
                        <i class="bi bi-plus-circle me-2"></i> Add to Ledger
                    </button>
                </form>
            </div>
        </div>

        <div class="col-lg-7">
            <div class="glass-card">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="fw-bold mb-0">Expense History</h5>
                    <span class="badge rounded-pill bg-info text-dark"><%= expenses.length %> Entries</span>
                </div>

                <div class="expense-list" style="max-height: 450px; overflow-y: auto; padding-right: 5px;">
                    <% if (expenses.length === 0) { %>
                        <div class="text-center py-5 opacity-50">
                            <i class="bi bi-receipt fs-1 d-block mb-2"></i>
                            No expenses recorded this month.
                        </div>
                    <% } else { %>
                        <% expenses.slice().reverse().forEach(exp => { %>
                            <div class="expense-item">
                                <div class="d-flex align-items-center">
                                    <div class="cat-icon me-3">
                                        <i class="bi bi-lightning-fill"></i>
                                    </div>
                                    <div class="flex-grow-1">
                                        <div class="d-flex justify-content-between align-items-start">
                                            <div>
                                                <span class="fw-bold text-uppercase small" style="letter-spacing: 1px;">
                                                    <%= exp.category %>
                                                </span>
                                                <div class="small opacity-50"><%= new Date(exp.date).toDateString() %></div>
                                            </div>
                                            <div class="text-end">
                                                <div class="fw-bold">₹<%= exp.amount %></div>
                                                <form method="POST" action="/expenses/delete/<%= exp._id %>" onsubmit="return confirm('Remove this entry?')">
                                                    <button type="submit" class="btn-delete p-0">
                                                        <i class="bi bi-trash3 small"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </div>
                                        <div class="mt-1">
                                            <small class="text-info fst-italic"><%= exp.note || '' %></small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } %>
                </div>
            </div>

            <div class="glass-card">
                <h6 class="fw-bold mb-3"><i class="bi bi-file-earmark-pdf text-danger me-2"></i> ITR Filing Guidance</h6>
                <div class="row">
                    <div class="col-md-6">
                        <a href="https://www.incometax.gov.in/iec/foportal/" target="_blank" class="itr-link">
                            <i class="bi bi-link-45deg me-2"></i> Official IT Portal
                        </a>
                        <a href="https://cleartax.in/s/income-tax-return" target="_blank" class="itr-link">
                            <i class="bi bi-link-45deg me-2"></i> ClearTax Beginner Guide
                        </a>
                    </div>
                    <div class="col-md-6 border-start border-white border-opacity-10">
                        <p class="small opacity-50 mb-0">
                            <strong>Did you know?</strong> Gig workers filing under <strong>Section 44ADA</strong> (Presumptive Taxation) only pay tax on 50% of their gross receipts!
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

</body>
</html>