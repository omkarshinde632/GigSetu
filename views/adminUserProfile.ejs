<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.name %> | GigSetu Profile</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #ffffff;
            min-height: 100vh;
            padding: 50px 0;
        }

        .glass-panel {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 30px;
            padding: 40px;
        }

        .score-meter {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            
            background: conic-gradient(#00c6ff <%= user.gigScore %>%, rgba(255,255,255,0.1) 0),
            display: flex,
            align-items: center,
            justify-content: center,
            margin: 0 auto,
            position: relative,
        }

        .score-meter::before {
            content: "";
            position: absolute;
            width: 130px;
            height: 130px;
            background: #1a2a33; 
            border-radius: 50%;
        }

        .score-inner {
            position: relative;
            text-align: center;
        }

        .score-number {
            font-size: 2.5rem;
            font-weight: 800;
            color: #00c6ff;
            line-height: 1;
        }

        .info-card {
            background: rgba(0, 0, 0, 0.2);
            border-radius: 20px;
            padding: 20px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            height: 100%;
        }

        .data-label {
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: rgba(0, 198, 255, 0.7);
            margin-bottom: 5px;
            display: block;
        }

        .data-value {
            font-size: 1.1rem;
            font-weight: 600;
        }

        .status-pill {
            padding: 5px 15px;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 700;
        }

        .verified { background: rgba(46, 213, 115, 0.2); color: #2ed573; }
        .frozen { background: rgba(255, 71, 87, 0.2); color: #ff4757; }

        .back-btn {
            color: rgba(255,255,255,0.5);
            text-decoration: none;
            transition: 0.3s;
        }
        .back-btn:hover { color: #00c6ff; }
    </style>
</head>
<body>

<div class="container">
    <a href="/admin/users" class="back-btn mb-4 d-inline-block">
        <i class="bi bi-chevron-left"></i> Back to Directory
    </a>

    <div class="glass-panel shadow-lg">
        <div class="row">
            <div class="col-lg-4 text-center border-end border-white border-opacity-10">
                <div class="score-meter mb-4 shadow">
                    <div class="score-inner">
                        <span class="score-number"><%= user.gigScore %></span>
                        <div class="small opacity-50">GIG SCORE</div>
                    </div>
                </div>
                
                <h2 class="fw-bold mb-1"><%= user.name %></h2>
                <p class="opacity-50"><%= user.email %></p>
                
                <div class="mt-3">
                    <% if (user.accountFrozen) { %>
                        <span class="status-pill frozen"><i class="bi bi-snow"></i> ACCOUNT FROZEN</span>
                    <% } else { %>
                        <span class="status-pill verified">
                            <i class="bi bi-patch-check-fill"></i> <%= user.verificationStatus.toUpperCase() %>
                        </span>
                    <% } %>
                </div>

                <div class="mt-5 d-grid gap-2">
                    <button class="btn btn-primary rounded-pill py-2 fw-bold" style="background: linear-gradient(45deg, #00c6ff, #0072ff); border: none;">
                        Contact Worker
                    </button>
                    <form method="POST" action="/admin/<%= user.accountFrozen ? 'unfreeze' : 'freeze' %>/<%= user._id %>">
                        <button class="btn btn-outline-<%= user.accountFrozen ? 'success' : 'danger' %> rounded-pill py-2 w-100">
                            <%= user.accountFrozen ? 'Unfreeze Account' : 'Freeze Account' %>
                        </button>
                    </form>
                </div>
            </div>

            <div class="col-lg-8 ps-lg-5 mt-4 mt-lg-0">
                <h4 class="fw-bold mb-4">Financial Intelligence</h4>
                
                <div class="row g-3 mb-4">
                    <div class="col-md-6">
                        <div class="info-card">
                            <span class="data-label">Available Wallet</span>
                            <span class="data-value text-success">₹<%= user.walletBalance.toLocaleString() %></span>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="info-card">
                            <span class="data-label">Platform Identity</span>
                            <span class="data-value">#<%= user._id.toString().slice(-10).toUpperCase() %></span>
                        </div>
                    </div>
                </div>

                <h4 class="fw-bold mb-4">Risk Factors & Metrics</h4>
                <div class="row g-3">
                    <div class="col-md-4">
                        <div class="info-card">
                            <span class="data-label">Repayment Rate</span>
                            <span class="data-value <%= repaymentRate < 70 ? 'text-danger' : 'text-success' %>">
                                <%= repaymentRate %>%
                            </span>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="info-card">
                            <span class="data-label">Active Loans</span>
                            <span class="data-value">
                                <%= activeLoansCount.toString().padStart(2, '0') %>
                            </span>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="info-card">
                            <span class="data-label">Member Since</span>
                            <span class="data-value">
                                <%= new Date(user.createdAt).toLocaleDateString('en-US', { month: 'short', year: 'numeric' }) %>
                            </span>
                        </div>
                    </div>
                </div>

                <div class="mt-5 glass-card p-4 rounded-4" style="background: rgba(255,255,255,0.02); border: 1px dashed rgba(255,255,255,0.1);">
                    <h5 class="fw-bold mb-3"><i class="bi bi-activity text-info me-2"></i> Intelligence Observation</h5>
                    <p class="small opacity-50 mb-0">
                        Profile for <strong><%= user.name %></strong> is currently 
                        <span class="text-info text-uppercase"><%= user.verificationStatus %></span>. 
                        <% if (user.gigScore >= 80) { %>
                            User demonstrates high fiscal responsibility. Eligibility for premium loan brackets is <strong>Optimal</strong>.
                        <% } else if (user.gigScore >= 50) { %>
                            Score is within the standard threshold. Continue regular monitoring of income frequency.
                        <% } else { %>
                            Caution: GigScore below critical threshold. Account may require additional income verification before new credit disbursement.
                        <% } %>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

</body>
</html>