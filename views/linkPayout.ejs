<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Link Payout | GigSetu</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .glass-panel {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 30px;
            padding: 40px;
            width: 100%;
            max-width: 550px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.3);
        }

        .nav-pills .nav-link {
            color: rgba(255,255,255,0.6);
            border-radius: 12px;
            padding: 12px 20px;
            font-weight: 600;
            transition: 0.3s;
            border: 1px solid transparent;
        }

        .nav-pills .nav-link.active {
            background: rgba(0, 198, 255, 0.1);
            color: #00c6ff;
            border: 1px solid rgba(0, 198, 255, 0.3);
        }

        .form-control {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
            border-radius: 12px;
            padding: 12px 15px;
            margin-bottom: 15px;
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border-color: #00c6ff;
            box-shadow: 0 0 15px rgba(0, 198, 255, 0.2);
        }

        .btn-link-payout {
            background: linear-gradient(45deg, #00c6ff, #0072ff);
            border: none;
            color: white;
            font-weight: 700;
            padding: 14px;
            border-radius: 12px;
            width: 100%;
            margin-top: 10px;
            transition: 0.3s;
        }

        .btn-link-payout:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0, 198, 255, 0.3);
        }

        .security-badge {
            background: rgba(46, 213, 115, 0.1);
            color: #2ed573;
            font-size: 0.75rem;
            padding: 8px 15px;
            border-radius: 50px;
            display: inline-flex;
            align-items: center;
            margin-bottom: 25px;
        }

        .back-nav {
            text-decoration: none;
            color: rgba(255,255,255,0.5);
            font-size: 0.9rem;
            transition: 0.3s;
        }

        .back-nav:hover { color: #00c6ff; }
    </style>
</head>
<body>

<div class="glass-panel text-center">
    <div class="security-badge">
        <i class="bi bi-shield-lock-fill me-2"></i> 256-BIT ENCRYPTED PAYOUT LINKING
    </div>

    <h2 class="fw-bold mb-2">🔗 Payout Settings</h2>
    <p class="opacity-50 small mb-4">Choose where you want to receive your gig earnings.</p>

    <ul class="nav nav-pills mb-4 justify-content-center" id="payoutTabs" role="tablist">
        <li class="nav-item w-50" role="presentation">
            <button class="nav-link active w-100" id="upi-tab" data-bs-toggle="pill" data-bs-target="#upi-pane" type="button">
                <i class="bi bi-lightning-charge me-2"></i>UPI
            </button>
        </li>
        <li class="nav-item w-50" role="presentation">
            <button class="nav-link w-100" id="bank-tab" data-bs-toggle="pill" data-bs-target="#bank-pane" type="button">
                <i class="bi bi-bank me-2"></i>Bank
            </button>
        </li>
    </ul>

    <form method="POST" action="/payout/link">
        <div class="tab-content" id="payoutTabContent">
            <div class="tab-pane fade show active" id="upi-pane" role="tabpanel">
                <div class="text-start">
                    <label class="small opacity-50 mb-2">UPI ID (VPA)</label>
                    <input type="text" name="upiId" class="form-control" placeholder="e.g. mobileNumber@ybl" pattern="^[\w.-]+@[\w.-]+$">
                    <div class="small opacity-50 mt-2">
                        <i class="bi bi-info-circle me-1"></i> Supports GPay, PhonePe, Paytm, and BHIM.
                    </div>
                </div>
            </div>

            <div class="tab-pane fade" id="bank-pane" role="tabpanel">
                <div class="text-start">
                    <label class="small opacity-50 mb-2">Bank Details</label>
                    <input type="text" name="bankName" class="form-control" placeholder="Full Bank Name">
                    <input type="text" name="accountNumber" class="form-control" placeholder="Account Number">
                    <input type="text" name="ifsc" class="form-control" placeholder="IFSC Code" style="text-transform: uppercase;">
                </div>
            </div>
        </div>

        <button type="submit" class="btn-link-payout">
            Save Payout Method
        </button>
    </form>

    <div class="mt-4">
        <a href="/dashboard" class="back-nav">
            <i class="bi bi-arrow-left me-1"></i> Return to Dashboard
        </a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>