<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fraud Intelligence | GigSetu</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: white;
            min-height: 100vh;
            padding: 40px 0;
        }

        .glass-panel {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.4);
        }

        .risk-table {
            color: white;
            border-collapse: separate;
            border-spacing: 0 10px;
        }

        .risk-table thead th {
            border: none;
            text-transform: uppercase;
            font-size: 12px;
            letter-spacing: 1px;
            opacity: 0.6;
            padding-bottom: 20px;
        }

        .risk-row {
            background: rgba(255, 255, 255, 0.03);
            transition: all 0.3s ease;
        }

        .risk-row:hover {
            background: rgba(220, 53, 69, 0.1);
            transform: scale(1.01);
        }

        .risk-row td {
            padding: 20px;
            border: none;
            vertical-align: middle;
        }

        .risk-row td:first-child { border-radius: 12px 0 0 12px; }
        .risk-row td:last-child { border-radius: 0 12px 12px 0; }

        .gig-score-bar {
            height: 8px;
            border-radius: 10px;
            background: rgba(255,255,255,0.1);
        }

        .badge-risk {
            background: rgba(220, 53, 69, 0.2);
            color: #ff6b6b;
            border: 1px solid #dc3545;
            padding: 6px 12px;
            border-radius: 8px;
            font-size: 11px;
            font-weight: 700;
        }

        .back-link {
            text-decoration: none;
            color: rgba(255,255,255,0.6);
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 20px;
            transition: 0.3s;
        }

        .back-link:hover { color: #00c6ff; }

        .empty-state {
            text-align: center;
            padding: 60px;
            opacity: 0.6;
        }
    </style>
</head>
<body>

    <%- include("partials/adminHeader", { title: "Home - GigSetu" }) %>

<div class="container">
    <a href="/admin/dashboard" class="back-link">
        <i class="bi bi-arrow-left"></i> Return to Command Center
    </a>

    <div class="glass-panel">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <div>
                <h2 class="fw-bold mb-1">🚨 Fraud Risk Monitoring</h2>
                <p class="text-info mb-0">Real-time AI behavioral analysis and anomaly detection</p>
            </div>
            <span class="badge bg-danger p-2 px-3">Live Feed</span>
        </div>

        <% if (users.length === 0) { %>
            <div class="empty-state">
                <i class="bi bi-shield-check" style="font-size: 4rem; color: #2ecc71;"></i>
                <h4 class="mt-3">System Clear</h4>
                <p>No high-risk anomalies detected in the current cycle.</p>
            </div>
        <% } else { %>
            <div class="table-responsive">
                <table class="table risk-table">
                    <thead>
                        <tr>
                            <th>User Identification</th>
                            <th>GigScore (Risk Indicator)</th>
                            <th>Verification Status</th>
                            <th>Threat Level</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(user => { %>
                            <tr class="risk-row">
                                <td>
                                    <div class="fw-bold"><%= user.name %></div>
                                    <small class="opacity-50">UID: <%= user._id.toString().slice(-6) %></small>
                                </td>
                                <td style="width: 250px;">
                                    <div class="d-flex justify-content-between mb-1">
                                        <small><%= user.gigScore %></small>
                                        <small><%= user.gigScore < 50 ? 'High Risk' : 'Moderate' %></small>
                                    </div>
                                    <div class="progress gig-score-bar">
                                        <div class="progress-bar <%= user.gigScore < 50 ? 'bg-danger' : 'bg-warning' %>" 
                                             role="progressbar" 
                                             style="width: <%= user.gigScore %>%"></div>
                                    </div>
                                </td>
                                <td>
                                    <span class="text-capitalize"><%= user.verificationStatus %></span>
                                </td>
                                <td>
                                    <span class="badge badge-risk">CRITICAL ANOMALY</span>
                                </td>
                                <td>
                                    <button class="btn btn-sm btn-outline-light">Investigate</button>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </div>
</div>

</body>
<%- include("partials/footer") %>
</html>