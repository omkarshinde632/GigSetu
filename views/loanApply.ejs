<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instant Credit | GigSetu</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #ffffff;
            min-height: 100vh;
            padding: 40px 0;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 30px;
            height: 100%;
        }

        .tier-badge {
            padding: 5px 15px;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .tier-low { background: #2ed573; color: #000; }
        .tier-mid { background: #ffa502; color: #000; }
        .tier-high { background: #ff4757; color: #fff; }

        .form-label { font-size: 0.9rem; opacity: 0.7; margin-bottom: 8px; }
        
        .form-control, .form-select {
            background: rgba(255, 255, 255, 0.05) !important;
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: #00c6ff !important;
            border-radius: 12px;
            padding: 12px;
            font-weight: 600;
        }

        .form-control::placeholder {
            color: rgba(255, 255, 255, 0.3) !important;
            font-weight: 400;
        }

        .form-select option { background: #203a43; color: white; }

        .input-group-text {
            background: rgba(255, 255, 255, 0.08);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: #00c6ff;
            font-weight: 600;
        }

        .btn-apply {
            background: linear-gradient(45deg, #00c6ff, #0072ff);
            border: none;
            color: white;
            font-weight: 700;
            padding: 15px;
            border-radius: 15px;
            transition: 0.3s;
            width: 100%;
        }
        .btn-apply:hover { transform: translateY(-3px); box-shadow: 0 10px 20px rgba(0, 198, 255, 0.3); }

        .repayment-box {
            background: rgba(0, 198, 255, 0.1);
            border: 1px dashed rgba(0, 198, 255, 0.3);
            border-radius: 15px;
            padding: 15px;
        }

        .error-container {
            max-width: 500px;
            margin: 50px auto;
            text-align: center;
        }
    </style>
</head>
<body>

    <%- include("partials/header", { title: "Home - GigSetu" }) %>

<div class="container">
    <% if (!eligibility.eligible) { %>
        <div class="error-container glass-card">
            <i class="bi bi-shield-exclamation text-warning" style="font-size: 4rem;"></i>
            <h2 class="fw-bold mt-3">Application Deferred</h2>
            <p class="opacity-75"><%= eligibility.reason %></p>
            <a href="/dashboard" class="btn btn-outline-light rounded-pill mt-3">Return to Dashboard</a>
        </div>
    <% } else { %>

        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="row g-4">
                    
                    <div class="col-md-5">
                        <div class="glass-card">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h5 class="fw-bold mb-0">Credit Profile</h5>
                                <span class="tier-badge tier-<%= eligibility.riskTier.toLowerCase() %>">
                                    <%= eligibility.riskTier %> RISK
                                </span>
                            </div>

                            <div class="mb-4">
                                <label class="form-label d-block">Maximum Loan Limit</label>
                                <h1 class="fw-bold text-info">₹<%= eligibility.loanLimit.toLocaleString() %></h1>
                            </div>

                            <hr class="opacity-10">

                            <div class="row g-3">
                                <div class="col-6">
                                    <small class="opacity-50 d-block">Avg. Monthly Earning</small>
                                    <span class="fw-bold text-white">₹<%= eligibility.avgMonthlyEarning.toLocaleString() %></span>
                                </div>
                                <div class="col-6">
                                    <small class="opacity-50 d-block">Annual Interest</small>
                                    <span class="fw-bold text-success"><%= (eligibility.interestRate * 100).toFixed(1) %>%</span>
                                </div>
                            </div>

                            <div class="mt-4 p-3 rounded-4 bg-white bg-opacity-10">
                                <small class="d-block opacity-75"><i class="bi bi-info-circle me-1"></i> GigSetu Tip:</small>
                                <p class="small mb-0">Repaying on time boosts your credit score and unlocks higher limits!</p>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-7">
                        <div class="glass-card">
                            <h4 class="fw-bold mb-4">Apply for Instant Credit</h4>
                            
                            <form method="POST" action="/loan/apply">
                                <div class="mb-4">
                                    <label class="form-label">Desired Amount</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-transparent border-end-0">₹</span>
                                        <input type="number" 
                                               name="amount" 
                                               class="form-control form-control-lg border-start-0 shadow-none" 
                                               max="<%= eligibility.loanLimit %>" 
                                               placeholder="Enter Amount"
                                               required />
                                    </div>
                                    <div class="form-text text-white-50 mt-2">
                                        Limit: ₹<%= eligibility.loanLimit.toLocaleString() %>
                                    </div>
                                </div>

                                <div class="mb-4">
                                    <label class="form-label">Repayment Tenure</label>
                                    <select name="tenure" class="form-select form-select-lg shadow-none">
                                        <option value="3">3 Months</option>
                                        <option value="6" selected>6 Months (Recommended)</option>
                                        <option value="12">12 Months</option>
                                    </select>
                                </div>

                                <div class="repayment-box mb-4">
                                    <div class="d-flex justify-content-between mb-2">
                                        <span class="small opacity-75">Estimated EMI</span>
                                        <span class="fw-bold text-info" id="emiDisplay">₹ --</span>
                                    </div>
                                    <div class="d-flex justify-content-between">
                                        <span class="small opacity-75">Total Repayment</span>
                                        <span class="small fw-bold" id="totalDisplay">₹ --</span>
                                    </div>
                                </div>

                                <button type="submit" class="btn-apply shadow">
                                    Confirm & Disburse Funds
                                </button>
                                
                                <p class="text-center small opacity-50 mt-3 mb-0">
                                    <i class="bi bi-clock-history me-1"></i> Funds arrive in 10-15 minutes.
                                </p>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    <% } %>
</div>

<script>
    const amountInput = document.querySelector('input[name="amount"]');
    const tenureSelect = document.querySelector('select[name="tenure"]');
    const emiDisplay = document.getElementById('emiDisplay');
    const totalDisplay = document.getElementById('totalDisplay');
    const rate = <%= eligibility.interestRate %>;

    function updatePreview() {
        const p = parseFloat(amountInput.value) || 0;
        const t = parseInt(tenureSelect.value);
        if (p > 0) {
            const total = p + (p * rate); 
            const emi = total / t;
            emiDisplay.innerText = '₹' + Math.round(emi).toLocaleString();
            totalDisplay.innerText = '₹' + Math.round(total).toLocaleString();
        } else {
            emiDisplay.innerText = '₹ --';
            totalDisplay.innerText = '₹ --';
        }
    }

    amountInput.addEventListener('input', updatePreview);
    tenureSelect.addEventListener('change', updatePreview);
</script>

</body>
<%- include("partials/footer") %>
</html>