<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Directory | GigSetu Intelligence</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #ffffff;
            min-height: 100vh;
            padding: 40px 0;
        }

        .back-nav {
            color: #00c6ff;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 600;
            transition: 0.3s;
            display: inline-flex;
            align-items: center;
            margin-bottom: 2rem;
        }

        .back-nav:hover { opacity: 0.7; transform: translateX(-5px); }

        /* User Card Design */
        .user-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 25px;
            height: 100%;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .user-card:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-8px);
            border-color: rgba(0, 198, 255, 0.3);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
        }

        /* Avatar Placeholder */
        .avatar-circle {
            width: 50px;
            height: 50px;
            background: linear-gradient(45deg, #00c6ff, #0072ff);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 1.2rem;
            margin-bottom: 15px;
        }

        .status-badge {
            font-size: 0.7rem;
            padding: 4px 10px;
            border-radius: 100px;
            text-transform: uppercase;
            font-weight: 700;
            letter-spacing: 0.5px;
        }

        .status-verified { background: rgba(46, 213, 115, 0.2); color: #2ed573; border: 1px solid #2ed573; }
        .status-pending { background: rgba(255, 165, 2, 0.2); color: #ffa502; border: 1px solid #ffa502; }

        .data-label {
            font-size: 0.75rem;
            color: rgba(255, 255, 255, 0.5);
            text-transform: uppercase;
            margin-top: 10px;
        }

        .data-value {
            font-weight: 600;
            display: block;
        }

        .gig-score-display {
            position: absolute;
            top: 25px;
            right: 25px;
            text-align: right;
        }

        .score-circle {
            font-size: 1.5rem;
            font-weight: 800;
            color: #00c6ff;
            line-height: 1;
        }
    </style>
</head>
<body>

    <%- include("partials/adminHeader", { title: "Home - GigSetu" }) %>

<div class="container">
    <div class="row align-items-end mb-4">
        <div class="col-md-8">
            <a href="/admin/dashboard" class="back-nav">
                <i class="bi bi-arrow-left me-2"></i> COMMAND CENTER
            </a>
            <h1 class="fw-bold mb-0">👥 User Management</h1>
            <p class="opacity-50 mt-2">Overseeing the growth and stability of the GigSetu ecosystem.</p>
        </div>
        <div class="col-md-4 text-md-end mt-3 mt-md-0">
            <span class="text-white-50">Total Enrolled:</span>
            <span class="fs-4 fw-bold ms-2 text-info"><%= users.length %></span>
        </div>
    </div>

    <div class="row g-4">
        <% users.forEach(user => { %>
            <div class="col-lg-4 col-md-6">
                <div class="user-card">
                    <div class="gig-score-display">
                        <div class="data-label">GigScore</div>
                        <div class="score-circle"><%= user.gigScore %></div>
                    </div>

                    <div class="avatar-circle">
                        <%= user.name.charAt(0).toUpperCase() %>
                    </div>
                    
                    <h5 class="fw-bold text-white mb-1"><%= user.name %></h5>
                    <p class="small opacity-50 mb-3"><%= user.email %></p>

                    <span class="status-badge <%= user.verificationStatus === 'verified' ? 'status-verified' : 'status-pending' %>">
                        <i class="bi <%= user.verificationStatus === 'verified' ? 'bi-patch-check-fill' : 'bi-clock-history' %> me-1"></i>
                        <%= user.verificationStatus %>
                    </span>

                    <hr class="my-3 opacity-10">

                    <div class="row">
                        <div class="col-6">
                            <span class="data-label">Wallet Balance</span>
                            <span class="data-value">₹<%= user.walletBalance.toLocaleString() %></span>
                        </div>
                        <div class="col-6">
                            <span class="data-label">Account UID</span>
                            <span class="data-value text-info">#<%= user._id.toString().slice(-5).toUpperCase() %></span>
                        </div>
                    </div>

                    <div class="mt-4">
    <a href="/admin/users/<%= user._id %>" class="btn btn-sm btn-outline-info w-100 rounded-pill py-2">
        <i class="bi bi-eye me-1"></i> View Full Profile
    </a>
</div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

</body>
<%- include("partials/footer") %>
</html>